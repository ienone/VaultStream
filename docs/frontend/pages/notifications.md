# 消息盒子

## 文档状态

active

## 当前代码

- 页面：`frontend/lib/features/notifications/notification_center_page.dart`
- 路由：`/notifications`

## 用户任务与当前界面

消息盒子集中展示持久化任务回执与需要注意的失败，不再把后台 diagnostics 临时拼成无语义地址的 bottom sheet。页面顶部按消息类别和状态筛选；列表展示发生了什么、时间、合并次数和一个详情入口。

用户可以全部已读，也可对单条消息标记已读/未读、静默、稍后提醒、移除或从全部记录中恢复。所有状态写回后端。入口徽标只显示真实未读数；SSE 是刷新提示，Web 在页面被使用时保留低频轮询兜底。

当前后端生产后台失败、用户主动任务完成回执、Agent 工具确认、Telegram Bot 显式捕获成功回执、平台账号登录失效和周期摘要消息。待确认消息跳转所属 `/agent?session_id=...` 会话，决定完成或所属运行/会话取消后不再出现在活动消息中；Telegram Bot `/ai` 发起的确认保留 Bot 来源，也可在原 Bot 消息通过归属受控的按钮决定。`/save` 的链接、文字和附件只有在内容提交成功后才产生“捕获回执”，可按类别筛选并跳转 `/collection/:id`；删除内容后回执自动移出。账号提醒跳转 `/accounts`，由手动检测、扫码登录、主动退出和 Cookie 保活的真实结果驱动；未配置不提醒，同一连续失效不重复刷屏，恢复后自动移出。周期摘要只汇总持久化动态和事件变化，空窗口不生成占位消息。普通 Bot 对话仍不作为消息生产者。

## 自适应行为

- 手机、短横屏和窄窗口使用全屏语义页面，筛选控件自动换行，列表保持单列。
- 宽屏仍是独立页面，但消息列限制最大宽度，避免正文横向拉伸。
- 页面支持下拉刷新、加载、空状态和错误重试；系统返回恢复真实来源页。

## API 关联

- `GET /api/v1/notifications`
- `POST /api/v1/notifications/{id}/actions`
- `POST /api/v1/notifications/read-all`
- `POST /api/v1/notifications/digest`

## 不承担职责

- 不内嵌完整任务日志、账号修复表单或 Agent 对话。
- 不根据自由文本猜测重试、确认或外部发送动作。
