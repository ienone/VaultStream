# 评估数据

## 文档状态

active

本目录由旧 `docs/eval/` 迁移而来。

## 文件

- `rag_ground_truth.json`: RAG/语义检索评估基准。
- `rag_recall_report.json`: RAG/语义检索召回结果。

## 使用规则

评估数据用于验证搜索/RAG 行为。修改评估数据时，应同步记录：

- 数据来源。
- 评估命令。
- 模型/配置版本。
- 结果变化。
