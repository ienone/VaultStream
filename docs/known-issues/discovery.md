1. 横屏状态下，右侧内容区域的横竖屏切换判断条件应该是仅依据右侧内容区域的宽不是整个应用的宽。注意到左侧的routers和条目栏的宽度是固定的，所以右侧内容区域的宽度如果无法直接获取，可以通过计算整个应用的宽度减去左侧固定宽度来得到。
2. 搜索框展开后的宽度要限制在左栏区域且与交界线的距离控制与左边框距离一致。考虑同步修改discovery页面的搜索框和收藏库的搜索框
3. 筛选的按钮也修改成和搜索框一样的动画样式，而不是底部弹出。
4. 探索界面的列表为空时显示空状态提示，提示内容可以参考收藏库的空状态提示，或者直接复用收藏库的空状态组件。
5. 检查探索界面筛选面板中的按来源筛选逻辑，似乎api返回列表中没有就不显示此项了。参照收藏库中按平台筛选的逻辑排查问题
6. 探索界面顶部栏与下方内容之间的分界线有一条实线分割，我希望移除以保持统一感