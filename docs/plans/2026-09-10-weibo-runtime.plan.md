# 微博真实解析运行修复

状态：已完成本轮运行修复；不代表微博所有内容类型验收完成。进度见同名 process。

真实短帖和人民日报主页解析成功；并发回归证明两个 async 解析入口直接执行 requests，慢请求期间其他协程不能运行。

本轮将既有同步解析流程移入工作线程，保持 URL、Cookie、代理、输出和错误类型不变；验证慢网络下其他协程继续运行，并复验相同真实链接。不新增平台接口或开启同步/分发。

复验中发现主页把抓取时刻填入 published_at，附带移除该伪造发布时间；账号原始资料继续保留。
