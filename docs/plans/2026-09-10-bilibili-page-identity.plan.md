# Bilibili 视频分 P 收藏身份

状态：本计划身份归一范围已实现并验证。

真实 SQLite 收藏入口复现：同一视频普通链接与尾斜杠 + p=1 创建两条内容。目标：视频路径统一 HTTPS/www、统一尾斜杠，默认第一页省略 p，其他正整数 p 保留并去除前导零；追踪参数不进入 canonical。不同分 P 不能合并。

复用既有视频 URL 范围，不扩展 AV/BV 转换；无效分 P 明确报错，不静默选择第一页。验证使用真实 ContentService/SQLite 和现有视频分 P 媒体回归。

追加真实验收发现：多分 P 返回正确 cid/时长，但标题始终为合集标题。补充平台 pages.part 和分 P 序号到多分 P 展示标题，单视频保持原名。真实样本 BV1Gr4y187aS 的前两节为课程简介、01 线性方程组；不下载整套课程。
