# Bilibili 真实视频与时间点内容

状态：执行中。属于持续真实场景验收 Goal，依据系统构想的视频、字幕检索和统一播放要求。进度见同名 process。

## 已有证据

当前 video_parser 只保存封面；真实样本 BV1z2421P7wV 的 view、player/playurl、player/v2 均返回 code=0。播放 MP4 首 1024 字节返回 206，player/v2 有 10 个章节和 661 条可读取的 ai-zh 字幕。详见真实验收 issue。

## 实施范围

1. 在现有 Bilibili 解析器获取选定分 P 的 cid、时长、播放来源、平台章节与字幕；保留来源语言与 AI 字幕性质。平台可选能力失败时保留已获取正文，并明确记录缺失，不能虚构时间点或成功。
2. 使用平台视频及分 P 身份关联媒体和时间片。资产入库后明确绑定同内容的 media_asset_id，复用现有 media_segments 校验及搜索 contract；不得按“第一个资产”等位置猜测。
3. 保持媒体归档总开关、视频子开关及大小上限。临时 CDN 地址不能成为稳定媒体身份；在线可播放性、失效刷新、离线保存分别验证，不为验收绕过开关。
4. 真实样本通过正式分享/解析路径进入库，验证字幕正文及章节保存、时间点搜索引用、视频播放与 seek。核对重解析不破坏媒体书签和引用身份。

## 验收边界

- 正式回归优先保护资产归属、稳定身份、书签数据及外部副作用边界。一次性平台探针用后删除。
- 不默认下载完整视频，不启用自动同步或外部分发。
- Web 真实播放与下载需要当前浏览器可用；Mac 锁屏期间先完成可独立进行的代码和后端验收，不声称视觉验收完成。
