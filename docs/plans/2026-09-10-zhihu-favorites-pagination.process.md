# 知乎收藏同步续页执行记录

当前范围已实现并验证：收藏夹 ID/offset 游标、页内上限、跨收藏夹续读、非法游标明确报错。固定 API 边界回归通过，真实账号连续两批各 3 条共 6 个不同收藏 ID，均有下一页。详见 `docs/issues/2026-09-10-real-world-acceptance.md`。

未开启自动同步，未把抽样导入数据库。上游排序并发变化及完整全量同步未验收，不能宣称全平台闭环完成。
