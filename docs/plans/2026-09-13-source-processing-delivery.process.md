# 来源接入、处理与分发执行记录

## 当前状态

in_progress。上一阶段完成后，按用户要求继续审查完整同步、恢复、来源变动与聚合更新；当前不将小批实测外推为全量验收。对应 [实施计划](2026-09-13-source-processing-delivery.plan.md)。

账号和合理模型调用已授权。X Cookie 已保存、格式有效，书签续读与同会话推文解析均已通过，**没有 X 凭据缺失阻塞**。未提交、推送、部署或发送外部消息。

## 当前能力与证据

| 范围 | 当前实现 | 已验证 | 剩余审查 |
| --- | --- | --- | --- |
| 微博 | 共享 SUB 会话、账号识别、分页/页内断点、条件刷新 | 真实两批各两条、无重叠、一条正文与媒体解析 | 到尾及收藏变动时恢复 |
| X | 显式保存 auth_token/ct0；网页 Bookmarks/TweetDetail；无独立 CLI、无已登录失败降级 | Cookie 格式、真实两批各两条、无重叠、同会话正文解析 | 上游游标到尾及完整同步 |
| Bilibili | 本人视频收藏夹、跨集合/页内断点、集合来源、不可用项说明 | 本人真实两条及集合身份；公开接口、隔离多集合与分页 | 收藏变动、完整同步与恢复 |
| 知乎/小红书 | 既有收藏接入、持久会话刷新、分页异常拒绝推进；知乎保留集合来源 | 各真实两条，返回续接游标；分页与会话回归 | 同上；小红书不提供收藏专辑 |
| RSS/TG | 同步失败不推进；TG 增量跨页、纯媒体、图片组及真实视频分类 | TG 两次请求跨页29条唯一有序；真实任务隔离入库20条/13条视频；RSS链路隔离验收 | 入库/后处理失败恢复 |
| 持久登录 | 重新登录/退出取消旧任务；先保存再成功；条件刷新；删除后清缓存 | 实际隔离数据库、重建服务与竞争回归；五平台实际读取 | 账号控制面与模块边界 |
| 自动聚合 | 20条新来源+10条近七天上下文；候选自动事件；版本检查、原子保存/游标、独立推送开关 | 真实模型续接/引句/排除无关指令；另一真实模型完整隔离落库1稿/3成员/游标；人工修改保护回归 | 来源更新、任务中断、跨批持续可用性 |
| 分发/Agent | 策略与目标检查；发送领取与API状态变更/强制入队的条件更新 | 真实隔离队列/渲染/pushed record；单项/批量/读取后领取等竞争回归 | 本轮变更的职责与重复实现复审 |

## 当前优先缺口

1. 已修复重复扫描追加同一来源：收藏边界按内容/来源/平台/集合幂等，不同集合保留。
2. 已修复提交后入队失败的恢复：合并与跳过都重试解析交接，不再追加来源；已删除内容跳过。失败项API和前端传回集合信息，重复成功重试不报500。
3. 继续核对各分页契约的末页、页内/跨集合恢复及收藏变动；不能仅靠两条读取判定完整同步。
4. 审查跨批聚合对来源更新、人工操作和中断的实际恢复路径，再决定是否需要修改。

## 已有验收基线

- 后端完整回归最近一次：150 passed，4.46秒；新修复后按相关范围复测。
- OpenAPI：150 endpoints / 57 action contracts；git diff --check 通过。
- 沙盒外 flutter analyze 无问题；flutter test 21 passed / 1 skipped。
- 收藏/聚合控制面已有验收；X登录对话框390×844、844×390、1200×800正常字号的遮蔽输入、校验、取消通过，临时文件已删除。
- 首次真实模型样本2582 tokens，正确续接候选事件、引用原文并排除无关材料内指令；第二次真实模型调用经过完整聚合服务保存临时库。未将私人收藏送入模型，未改生产内容/开关，凭据不写入验收数据库。
- TG视频归档复用现有图片/视频开关及数量/大小限制，仅替换成功项；关闭视频归档时保留视频地址，相关数据完整性回归通过。
- 实际平台检查脚本均已删除；只保留必要的数量、行为结果，不保留凭据或私人收藏正文。

## X 二维码调查边界

X官方Passkey与FIDO跨设备认证依赖已注册Passkey、真实客户端及蓝牙近距离验证；个人主页二维码不是登录授权。匿名无头登录页跳到/i/jf/onboarding/web，观察期间未完整渲染表单，不能据此证明没有扫码入口。没有已验证的通用远程二维码登录方案，不将探索挤占核心同步。依据和限制见 [X平台文档](../knowledges/adapters/TWITTER_ADAPTER.md)。

## 工作区与交付边界

初始已有 Makefile、README.md、docs/issues/2026-09-13-ui-queue-date.md、docs/knowledges/ui-review-2026-09-13/，保留他人修改。协调任务负责旧归档及文档索引，本任务不重排docs目录。外部平台可能使会话失效，真实模型样本不证明普遍语义质量；外部发送仍需具体授权。

## 本轮修复验证

- 收藏恢复/分页/多集合相关回归22项通过；新增真实API单项重试与重复成功验收通过。共享ContentService的解析队列交接提取为同一方法，未增加替代入库路径。
- 知乎真实只读契约：收藏夹页16项、is_end=true；条目页20项、is_end=false，两者均为列表与布尔末页标记。未输出账号/内容数据，探针已删除。
- 中间完整回归158 passed；后续API/前端字段变更正在完成最后复检。
