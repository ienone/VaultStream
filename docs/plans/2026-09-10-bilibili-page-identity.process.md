# Bilibili 分 P 身份执行记录

真实 SQLite create_share 复现默认链接与 /?p=1 创建不同记录，修复后复用同一内容；p=2 独立，p=02 与 p=2 相同。空值、零、负数、非数值的 p 明确拒绝。

当前用户库唯一 Bilibili 内容 16 使用带尾斜杠 canonical，因此统一到该形式，不改其 ID 或媒体关联。完整后端 67 项通过；最后对尾斜杠形式的调整再通过两项目标回归。

真实多分 P 追加验收：BV1Gr4y187aS 前两节媒体 cid/时长不同且正确，但原标题均为合集名称。现标题包含 P1 课程简介 / P2 01 线性方程组，实际复验分别为 82/631 秒，未下载或导入整套课程。两节本次均无字幕 chunks，不宣称字幕链路已验证。
