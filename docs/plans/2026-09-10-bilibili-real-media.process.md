# Bilibili 真实媒体执行记录

2026-09-10，in_progress。

- 已实现按分 P cid 解析播放源、平台章节、字幕；真实解析结果为视频候选 1、章节 10、字幕 661。一次上游字幕列表为空，后续同账号复查和再次解析正常，未添加无依据自动重试。
- 已用真实 SQLite 复现重解析删除媒体资产并级联丢失书签，修复为明确身份复用。CDN 地址变更保留 ID；既有本地变体缺席于新解析结果时保留归档；时间片显式绑定同内容资产。
- 媒体身份、字幕 Cookie 边界、解析任务、manifest 与访问回归共 8 项通过。长期保留书签完整性、跨内容关联和凭据边界测试。
- 未完成正式分享入库、搜索、真实播放和 seek、远端地址失效刷新。当前逐字幕时间片会进入现有 embedding 单元流程，正式导入前需核对批量与成本，避免对 661 条短字幕逐条低效调用。
- 运行中的后端尚未重启加载此轮代码；前端保存功能仍待解除系统锁屏后的真实下载验收。没有把阶段实现当作端到端完成。

## 正式导入与质量复核

- 通过 `/shares` 导入内容 16，沿用当前已开启的视频归档设置。视频实际保存 52,632,504 字节；ffprobe 为 H.264 + AAC，640×360，1422.677 秒。抽取第 60 秒画面为黑洞/奇点科普，与目标视频一致。
- 正式首次解析暴露旧 player/v2 的字幕来源不可靠：保存的 36 条字幕内容在讲手表，与黑洞视频不符。改用 player/wbi/v2，验证返回的 bvid/cid，并与 view 已声明的字幕轨 ID 对照；没有添加旧接口 fallback。正式重解析 run `59451234a89f47148ca42e99dfd39127` 得到正确的 661 条 ai-zh 字幕。旧错误正文已从当前向量记录中清除。
- 相邻短字幕组成最长约 30 秒、最多约 800 字符的检索段（单条超长字幕保持完整），长间隔另起一段。每段仍保留全部原始 cues 和起止时间。本样本 47 个字幕段 + 10 个章节 + 1 个全局单元，共 58 个向量单元，避免 672 次逐短句调用。数据完整性回归核对每条文字和时间戳均可还原。
- 真实自动索引先完成 52 个，6 个遇到 Gemini 429。通过正式单项失败重试接口只补齐这 6 个，均返回 indexed，没有重跑已成功单元。
- 正式详情返回 57 个时间段；搜索“霍金辐射”命中 904 秒章节及相关字幕，“黑洞为什么会蒸发”以 vector 命中同一章节。签名视频资源不携带控制面 Token，Range 返回 206、bytes 0-1023/52632504。
- 当前相关回归 7 项通过。后端已经重启加载上述修复。浏览器仍因系统锁屏不可用，因此实际网页播放/seek、图集下载、跨页面播放还未复验；远端 CDN 失效刷新和其他平台样本继续推进。
