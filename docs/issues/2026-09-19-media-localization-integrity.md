# 图片归档与缓存残留问题

状态：归档主链、自动补齐入口及客户端缓存身份已完成本地实现与验收；旧服务器历史清理已执行，新版部署与业务库迁移尚未执行。当前实现见[媒体模块](../backend/modules/media.md)。

## 已处理

- 图片/视频共用下载和成功发布流程；解码或写入失败不重下，写入失败不发布 local 引用。
- 同键并发使用独立临时文件原子发布；同批相同 URL 复用处理结果。
- 再次处理会检查主文件存在性，缺失时重下；缺失缩略图从本地主文件重建。
- URL 保留原有百分号编码；正文替换匹配完整地址，避免破坏签名或误替换 URL 前缀。
- 删除图像 FFmpeg/Pillow 双路径和重复主色实现。Pillow 编码、缩略图和主色放入线程。
- 比较实际字节数，只保存较小的一个主文件；已有 WebP 不重复有损编码。小图直接复用主文件供列表使用。
- 解析与发现共用引用更新，保留部分归档失败或未启用归档的其他媒体。
- 删除 stored_images/stored_videos 重复列表，以及各平台生产、提取、引用与资产入库中的双路读取；状态计数直接来自原条目。旧数据通过 Alembic revision 一次合并，保留来源、角色、存储键和缩略图信息。
- 资产变体采用实际存储 MIME/尺寸/大小/校验和；缩略图不继承主图的元数据。

## 自动补齐与缓存（2026-09-24）

- 重复解析已成功内容、服务端确认本地文件损坏/缺失都会按归档开关排入现有持久任务队列；同内容只保留一个待执行或运行中的修复任务。修复只处理媒体，不重解析正文或触发摘要、推送；下载期间的编辑/删除受版本检查保护。
- 内容寻址文件下载或缩略图重建时原子发布，即使旧路径已有损坏文件也可恢复。
- 图片缓存使用文件版本键与账号/服务器作用域，签名变化复用 Flutter 已解码图片，账号或服务器变化隔离缓存。未新增磁盘缓存。
- 历史数据工具支持只读统计及 `--repair-media --apply` 入队；修复结果以任务账本和媒体状态为准。

## 待外部环境验收

已指定现有阿里云服务器及 Telegram Bot/目标频道进行验收。SSH 的 Fail2ban 误封已解除，旧 VaultStream 库已备份并清理过期订阅及独占媒体；尚未执行新版业务库迁移、批量修复或真实外发。服务器其他应用保持不变。

## 验证

2026-09-25 旧服务器维护：先备份业务库，再清理 24,903 条已过期且无收藏、分发或父子关系等保留依赖的订阅候选及独占媒体，保留 1,511 条；外键检查无错误、quick_check 为 ok。系统盘可用空间从约 361 MB 恢复至约 15 GB，其他应用数据未改动。备份位于服务器 `/opt/vaultstream/backups/vaultstream-before-cleanup-20260925.db`。

旧容器日志共 9,895,480 行，主要为 4,708,248 行模型构建、4,706,688 行无效密钥或余额不足评分错误、229,918 行成功健康检查。过期删除曾因外键约束失败 324 次。重复原始日志统计后删除，线上容器已应用 10 MB × 3 的上限。2026-09-25 新版代码和数据库迁移已部署；1,394 条历史内容回填 4,638 个图片资产、4,637 个本地变体，回填报告缺失本地变体为 0。抽查历史 WebP 的签名读取返回 200，未在生产环境人为损坏媒体做修复测试。

使用真实 Pillow、临时文件系统和资产数据库，数据迁移在独立 SQLite 中通过实际 Alembic upgrade 验证；网络仅替换边界：验证写失败不发布引用、同键并发原子写、缺文件恢复、缩略图本地重建、签名 URL 保真、部分归档不丢媒体，以及变体元数据与实际文件一致。

临时压缩验收：一张仓库 JPEG 样本 43,852 B → 25,164 B，缩略图 3,920 B；合成噪声 PNG 2,218,426 B → 458,840 B。另一个 JPEG 转码未节省空间，因此保留单份源图；110 B 的小 PNG 不增加缩略图。仅代表这些样本，不推断全库节省比例。临时脚本验收后删除。

追加验收覆盖真实文件缺失/损坏后的服务端核验、重复入队合并、执行修复、关闭开关、编辑竞态及停用媒体类型隔离。前端使用真实 HTTP 图片和 Flutter 解码缓存验证：签名变化不增加下载，文件版本、账号或服务器变化重新下载。
