# ====================================================================
# VaultStream 配置文件
# ====================================================================
# 只列出需要手动配置的项。其余配置均有合理默认值，见 app/core/config.py。

# ====================================================================
# 运行环境（生产环境必须修改）
# ====================================================================
APP_ENV=dev
DEBUG=True

# API 鉴权密钥（install.sh 会自动生成；生产环境必须设置）
API_TOKEN=

# CORS 允许的来源（逗号分隔；生产环境必须设置具体域名）
# 示例: CORS_ALLOWED_ORIGINS=https://vault.example.com
CORS_ALLOWED_ORIGINS=*

# 外部访问的基础 URL（用于生成媒体链接等，按实际部署地址填写）
# BASE_URL=https://vault.example.com
# STORAGE_PUBLIC_BASE_URL=https://vault.example.com/api/v1/media

# ====================================================================
# Telegram Bot（可选）
# ====================================================================
# Bot 账号和 Token 通过 /api/v1/bot-config API 管理
# 以下仅控制 Bot 的使用权限
# 管理员用户 ID（逗号分隔）
TELEGRAM_ADMIN_IDS=
# 白名单用户 ID（为空则允许所有用户）
TELEGRAM_WHITELIST_IDS=
# 黑名单用户 ID
TELEGRAM_BLACKLIST_IDS=

# ====================================================================
# LLM 配置（可选，用于通用适配器智能提取）
# ====================================================================

# 文本模型（用于网页清洗、摘要生成）
TEXT_LLM_API_KEY=
TEXT_LLM_BASE_URL=https://api.deepseek.com
TEXT_LLM_MODEL=deepseek-chat

# 视觉模型（用于复杂网页分析）
VISION_LLM_API_KEY=
VISION_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
VISION_LLM_MODEL=qwen-vl-max

# ====================================================================
# Napcat / QQ Bot（可选）
# ====================================================================
ENABLE_NAPCAT=False
# NAPCAT_API_BASE=http://127.0.0.1:3000
# NAPCAT_ACCESS_TOKEN=
# NAPCAT_BOT_UIN=

# ====================================================================
# 代理（可选，用于访问 Telegram / Twitter 等）
# ====================================================================
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890

# ====================================================================
# 平台 Cookie（可选，用于需要登录的内容）
# ====================================================================
# BILIBILI_SESSDATA=
# XIAOHONGSHU_COOKIE=
# ZHIHU_COOKIE=
# WEIBO_COOKIE=
