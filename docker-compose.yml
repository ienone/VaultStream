# VaultStream 轻量模式部署
# 当前不需要 Docker 服务，直接运行 ./start.sh 即可

# 如需使用 Docker 运行应用本身，可取消下面的注释：
# services:
#   app:
#     build: .
#     container_name: vaultstream_app
#     ports:
#       - "8000:8000"
#     volumes:
#       - ./data:/app/data  # SQLite 数据库 + 本地媒体存储
#       - ./logs:/app/logs  # 日志文件
#     environment:
#       - DATABASE_TYPE=sqlite
#       - SQLITE_DB_PATH=/app/data/vaultstream.db
#       - QUEUE_TYPE=sqlite
#       - STORAGE_BACKEND=local
#       - STORAGE_LOCAL_ROOT=/app/data/media
#       - STORAGE_PUBLIC_BASE_URL=http://localhost:8000/api/v1
#     restart: unless-stopped

# ============================================================================
# PostgreSQL/Redis/MinIO 服务定义已移除
# ============================================================================
# 如需使用生产模式，请参考 git 历史或文档重新添加：
# - PostgreSQL 服务
# - Redis 服务  
# - MinIO 服务
# ============================================================================
